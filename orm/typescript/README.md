# vorm (TypeScript / JavaScript)

Planned. Same product model as Go:

- Schema builder → Vorzela `vm` migrations
- Hybrid `vorm:query` stubs → generated SQL (+ typed clients)

Not implemented yet. Use [`../go/`](../go/) for now.
